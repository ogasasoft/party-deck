# Mapillary Graph API token for local collection.
# Do not commit a real token.
MAPILLARY_ACCESS_TOKEN=

# Later, when the real Mapillary viewer is wired into the browser app.
VITE_MAPILLARY_ACCESS_TOKEN=
